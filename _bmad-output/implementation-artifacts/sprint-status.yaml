# generated: 2026-04-18
# last_updated: 2026-04-28 (Story 5.7 CR — bmad-code-review + 14 patches appliqués)
# project: sav-monorepo
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts
# phase: 2
# source: _bmad-output/planning-artifacts/epics.md
#
# Phase 1 (Suppression serveur Infomaniak) est archivée sous ./phase-1/
#
# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic pas encore démarré
#   - in-progress: Epic en cours de réalisation
#   - done: Toutes les stories de l'epic sont terminées
#
# Epic Status Transitions:
#   - backlog → in-progress: automatique à la création de la première story
#   - in-progress → done: manuel quand toutes les stories sont en 'done'
#
# Story Status:
#   - backlog: Story n'existe que dans epics.md
#   - ready-for-dev: Fichier story créé dans implementation-artifacts/
#   - in-progress: Dev actif sur l'implémentation
#   - review: Prêt pour code-review
#   - done: Story terminée et mergée
#
# Retrospective Status:
#   - optional: Peut être lancée mais non obligatoire
#   - done: Rétrospective effectuée
#
# WORKFLOW NOTES:
# ===============
# - Les stories sont ordonnées dans l'ordre logique de livraison de chaque epic
# - Dépendances inter-epic : 1 → 2 → 3 → 4/5/6 → 7
# - Une story passe par 'review' avant 'done' (via bmad-code-review)

generated: 2026-04-18
last_updated: 2026-04-29  # Story 6.6 DS+CR adversarial + Hardening complete → done
# 2026-04-29 : Epic 6 contextualisation complète — 7 stories créées ready-for-dev.
# 6.1 enrichissement email_outbox (placeholder Story 3.5 → V1 retry queue) +
#     vérification CHECK schéma JSONB notification_prefs + idx partiel
#     weekly_recap_optin. Migration additive, zéro régression Stories 3-5.
#     11 AC, tests SQL only.
# 6.2 landing magic-link `/monespace/auth` + liste SAV adhérent `/monespace`.
#     NFR-P6 < 10s clic→liste. Consolidation router op-based dans
#     `api/self-service/draft.ts` (3 nouvelles ops `me`, `sav-list`,
#     `sav-detail` placeholder) — cap Vercel 12/12 préservé. RLS member_id
#     filter app-side + DB defense. 15 AC.
# 6.3 détail SAV adhérent + commentaires bidirectionnels + fichiers.
#     Réutilise pipeline upload Graph API Story 2.4 (extension upload-complete
#     pour branchement savReference). Migration `sav_files.uploaded_by_*`
#     additionnelle. RLS sav_files/sav_comments authenticated.
#     Producteur email_outbox kind=`sav_comment_added`. 17 AC.
# 6.4 téléchargement PDF bon SAV (extension `pdfRedirectHandler` Story 4.4
#     polymorphique member/operator) + page préférences notifications
#     (toggle status_updates + weekly_recap, PATCH JSONB merge).
#     Cap Vercel inchangé (op `preferences` dans router self-service).
#     14 AC.
# 6.5 scope étendu responsable de groupe (onglets `/monespace`, query param
#     `scope=group` sur sav-list, RLS group_manager_scope, défense-en-profondeur
#     re-check DB `is_group_manager` au runtime, privacy email exclu).
#     13 AC.
# 6.6 envoi emails transactionnels via outbox + retry. Runner `retry-emails.ts`
#     intégré au dispatcher quotidien Vercel Hobby (cron 1/jour — trade-off
#     documenté). 8 templates pures `_lib/emails/transactional/*`. Backoff
#     exponentiel 1→16min cap 5 attempts. Multi-compte SMTP `noreply|sav`
#     via colonne `account` Story 5.7. Opt-out check `notification_prefs`.
#     Concurrency 5 + try/catch per-row. Migration RPC `transition_sav_status`
#     pour ajouter `template_data` JSONB + nouvelle RPC `enqueue_new_sav_alerts`
#     pour broadcast operator. 12 AC.
# 6.7 récap hebdo responsable opt-in. Runner `weekly-recap.ts` intégré
#     dispatcher (guard `getUTCDay()===5`). Template `transactional/
#     weekly-recap.ts`. Index UNIQUE dédup `(recipient_member_id,
#     date_trunc('week', created_at)) WHERE kind='weekly_recap'`. 10 AC.
# Ordre dev recommandé : 6.1 (foundation schéma) bloquant tout → 6.2 (landing+
# liste, débloque 6.3-6.5) → 6.3 + 6.4 + 6.5 en parallèle → 6.6 (consommateur,
# bénéficie producteurs 6.3) → 6.7 (dépend templates 6.6).
# Cap Vercel 12/12 préservé sur tout l'Epic 6 (consolidation op-based dans
# `api/self-service/draft.ts` + extension `pdfRedirectHandler` polymorphique).
# Cron Hobby 1/jour préservé (1 dispatcher quotidien étendu — pas de slot crons
# supplémentaire). Privacy NFR : `members.email` jamais exposé côté responsable.
# 2026-04-28 : Story 5.6 → review (validation empirique FR36). Ajout
# MARTINEZ = pur config (martinezConfig.ts + entry map) + endpoint
# /config-list dynamique. ZERO modif supplierExportBuilder.ts (guard CI
# vert). 12 tests nouveaux (5 martinez-config + 2 export-supplier MARTINEZ
# + 2 useSupplierExport fetchConfigList + 3 ExportSupplierModal Story 5.6).
# Suite 924/924, typecheck 0, build 460.72 KB ≡ baseline, lint:business 0.
# 2026-04-25 : session polish moyen — 8 W## résolus (cohérence settings,
# retry PDF Graph API, micro-fixes DB/UX). 6 commits atomiques sur
# refonte-phase-2 (à pusher séparément quand prêt) :
#   a83bef1 W22+W37 settings overlap guard : trigger BEFORE INSERT WHEN
#           NEW.valid_to IS NULL ferme automatiquement la version active
#           précédente (UPDATE ... SET valid_to = NEW.valid_from) +
#           partial UNIQUE INDEX settings(key) WHERE valid_to IS NULL —
#           défense en profondeur indépendante. Audit DB préview confirmé
#           zéro overlap pré-existante. Compat seeds idempotents (WHERE
#           NOT EXISTS skip naturel). Migration 20260504120000 + test SQL
#           3 cas (supersede + race unique + INSERT historique skip).
#   ceab551 W34+W35 PDF retry smart classification + MSAL refresh sur
#           401 : helper `isTransientGraphError(err)` exporté short-circuit
#           sur 400/403/404/413 + assertions locales (gain ~7s lambda
#           budget). Helper `forceRefreshAccessToken()` dans graph.js
#           via MSAL acquireTokenByClientCredential({skipCache:true}) —
#           refresh AVANT retry suivant sur 401, fail-soft si refresh KO.
#           4 tests Vitest ajoutés (W34 400 short-circuit, W34 503 retry,
#           W35 401→refresh+success, W35 401×3 raise sans boucle infinie).
#           Tests baseline G01-G10 préservés (10 → 14 tests fichier).
#   2538855 W26 trigger BEFORE UPDATE OF sav_id sav_lines IMMUTABLE :
#           raise IMMUTABLE_SAV_ID|line_id=...|old_sav_id=...|new_sav_id=...
#           ERRCODE check_violation. Défense en profondeur vs whitelist
#           update_sav_line (qui exclut déjà sav_id). Si besoin légitime
#           apparaît, DROP/RECREATE explicit + recompute des deux totaux
#           documenté. Migration 20260504130000 + test SQL 2 cas.
#   e688043 W28 resolveSettingAt tie-break déterministe (valid_from
#           DESC, id DESC) : SettingRow.id?: number optionnel, fallback
#           -Infinity (legacy callers first-vue wins préservé). Test
#           Vitest tie-break id=10/20 → id=20 wins quel que soit l'ordre
#           input. Complément W37 partial UNIQUE INDEX (qui interdit
#           désormais 2 rows actives → tie-break utile sur edge race).
#   a3584ec W18 formatter qty validation_message (PG to_char + TS
#           toFixed) : helper TS `formatQty(value)` exporté +
#           validation_message qty_exceeds utilise formatQty. Migration
#           20260504140000 mirror PG via regexp_replace(qty::text,
#           '\.?0+$', ''). 7 tests Vitest (1 message + 6 formatQty).
#   7c0e630 W8 LINES_BLOCKED ids array_to_string format pipe-friendly :
#           CREATE OR REPLACE FUNCTION transition_sav_status avec
#           array_to_string(v_blocked_ids, ',') → format
#           LINES_BLOCKED|ids=1,2,3 (au lieu de {1,2,3} bigint[] natif).
#           Body identique sauf bloc LINES_BLOCKED. SET search_path +
#           SET app.actor_operator_id reset (W2+W13) ré-incorporés
#           explicitement (CREATE OR REPLACE écrase ALTER précédents).
#           Migration 20260504150000 + test SQL 2 cas (multi 3 ids +
#           singleton). Compat caller LIKE 'LINES_BLOCKED%' préservée.
# Tests : 742/742 (vs baseline 730 : +12 nouveaux W34/W35 + W28 + W18 +
# 6 formatQty), typecheck 0, lint:business 0, build 459.96 KB stable
# (sous cap 460 KB AC #17 Story 5.2). Tests SQL : 3 nouveaux dans
# tests/security/ (w22_w37, w26, w8) exécutés en CI au push.
# 2026-04-25 : cross-cutting sécurité PG — W2 + W10 + W11 + W13 + W14 + W17
# résolus + W12 strikethrough (couvert Story 4.6 done). 4 commits atomiques
# sur refonte-phase-2 (à pusher séparément quand prêt) :
#   b5dbbd8 W14 RLS authenticated EXISTS operators active (4 policies :
#           credit_notes, sav, sav_lines, sav_files) — bloquant Epic 6 levé.
#           Migration 20260503120000 + test SQL 5 cas + extension CI runner
#           pour découvrir tests/security/*.sql.
#   16df7d3 W2+W10+W17 SET search_path lockdown + qualifications public.* :
#           ALTER FUNCTION × 9 RPCs SECURITY DEFINER manquantes + audit_changes
#           re-créé avec SET + INSERT INTO public.audit_trail qualifié +
#           recompute_sav_total avec FROM public.sav_lines + UPDATE public.sav.
#           Migration 20260503130000 + test introspection pg_proc.proconfig
#           (12 RPCs/triggers) + leurre pg_temp.audit_trail.
#   6197b04 W13 reset app.actor_operator_id en fin de RPC (mécanisme PG
#           ALTER FUNCTION SET app.actor_operator_id='' save/restore × 8
#           RPCs SECURITY DEFINER concernées). Defense-in-depth pgBouncer
#           sans réécrire les bodies (zéro drift). Migration 20260503140000
#           + test introspection + comportemental.
#   6d1b68e W11 purge_audit_pii_for_member helper RGPD curative (Option B).
#           Migration 20260503150000 + test 4 cas (purge before+after,
#           préservation autres members, idempotence, NULL guard). Câblage
#           Story Epic 7.6 future (admin-rgpd-export-json-signe-anonymisation).
# Tests : 730/730 inchangés (aucun TS modifié), typecheck 0, lint:business 0,
# build 459.96 KB stable (sous cap 460 KB AC #17 Story 5.2). Tests SQL :
# 4 nouveaux dans tests/security/ exécutés en CI au push. Strategie ALTER
# FUNCTION (W2 + W13) zéro réécriture body sur ~1500 lignes RPCs, mécanisme
# PG save/restore documenté dans les migrations. Spec : spec-cross-cutting-
# security-pg-hardening.md (status=done) + matrice RPC × patches pour
# traçabilité Epic 7 audit RGPD.
# 2026-04-25 : quick wins deferred-work post-5.2 — 9 W## résolus + Story 5.1
# coverage empirique. 6 commits atomiques sur refonte-phase-2 :
#   8b04c0e chore(deps): W36 install @vercel/functions ^3.4.4 prod (fix
#           wait-until.ts fallback silencieux post-PDF gen Vercel).
#   79d850f chore(deferred): W30+W31+W32 polish 4.3 (settings.unknown_value
#           _shape warn + ESLint patterns clients/** + globals XHR/WS +
#           Number.isNaN reject dans resolveSettingAt + 6 tests).
#   f4a79e8 chore(deferred): W19+W20+W21 polish 4.2 fixture/test isolation
#           (sqlLiteral boolean/bigint/array + 7 tests + Test 11 +1/-1 +
#           DELETE Tests 1-7).
#   01334d0 chore(deferred): W4 update_sav_line p_expected_version int→bigint
#           (migration 20260502120000).
#   4f74df8 docs(prd): W15 PG17 IMMUTABLE AT TIME ZONE 'UTC' patch §846.
#   53e596e chore(deferred): typecheck fixes + Story 5.1 empirical coverage
#           (86.14% global / 91.6% hors upload-export.ts mocké).
# Tests : 730/730 (+13 vs baseline 717 : 7 sqlLiteral, 3 ESLint, 2 settings
# Resolver NaN, 1 detail-handler unknown_value_shape). Typecheck 0,
# lint:business 0, build 459.96 KB (cap 460 KB inchangé AC #17 Story 5.2).
# 2026-04-25 : cleanup deferred-work post-5.2 — 12 W## traités sur 17 (W43,
# W53-W56 reportés, statut documenté inchangé). 3 commits atomiques sur
# refonte-phase-2 :
#   498c627 W40+W52 (router auth top-level draft.ts + METHOD_NOT_ALLOWED 405
#           cross-router) — sécurité + cross-cutting AVANT Stories 5.3-5.5.
#   031bdfa W41+W42+W46+W49+W50 (modal a11y focus-trap+ESC, composable refs
#           séparées generating/fetchingHistory + AbortController +
#           onScopeDispose, mapping 5xx→GATEWAY, historyLoadFailed flag).
#   cd0128e W44+W45+W47+W48+W51 (bench garde-fou BENCH_ALLOW_DESTRUCTIVE,
#           router.replace merge query, test no-retry uploadExportXlsx,
#           ?supplier= vide accepté, formatDate sur period_from/to).
# Tests : 717/717 (+13 nouveaux : W40 auth, W52×4 router 405 status, W41×2
# ESC+focus-trap, W42 indépendance refs, W46×2 abort, W49×2 GATEWAY 502/504,
# W50 historyLoadFailed, W47 no-retry, W48 supplier vide). Typecheck 0,
# lint:business 0, build 459.96 KB (sous cap 460 KB AC #17 Story 5.2).
# 2026-04-24 : Story 5.2 DS + CR adversarial 3 couches (Blind+Edge+Auditor).
# 17/17 AC couverts, 10 tasks done. 10 patches appliqués (P1-P10) :
# P1 rate-limit bucket partagé post-Zod (INVALID:{opId}) anti-bypass rotation
#    suppliers invalides, P2 cursor strict ISO regex + id>0 anti-injection
#    PostgREST .or(), P3 shortEmail null si @ absent (PII leak), P4 regex
#    stricte YYYY-MM-DD sur period_from/to + UTC-midnight normalisation
#    cohérente builder 5.1, P5 host allowlist OneDrive avant 302 anti-open-
#    redirect, P6 filename path-traversal guard upload-export, P7 CONFIG_ERROR|
#    préfixe → remap 500 EXPORTS_FOLDER_NOT_CONFIGURED au lieu de 502 trompeur,
#    P8 placeholder strict equality (vs startsWith faux-positif),
#    P9 double-click re-entry guard modal, P10 window.open au lieu de
#    window.location.href (préserve SPA). Tests CR +10 (704/704 post-CR,
#    baseline pré-5.2 661). Vercel slots 11/12 (consolidation self-service
#    draft.ts router -2 upload-*.ts + 1 pilotage.ts = -1 net). Bundle
#    459.91 KB stable post-CR (+0.75 KB gzip vs baseline pré-5.2, sous
#    cap 10 KB AC #17). 17 defers W40-W56 (router top-level auth, AbortController,
#    focus-trap + ESC modal, METHOD_NOT_ALLOWED 405 code, bench --dry-run,
#    upload-complete cap replace, path /admin vs /back-office décision
#    produit, bench réel différé cutover, etc.). Pattern consolidation
#    self-service documenté docs/api-contracts-vercel.md (référence Epic 6).
# 2026-04-24 : Epic 5 contextualisation complète — 6 stories créées ready-for-dev.
# 5.1 architecture export générique : builder TS agnostique fournisseur
# (SupplierExportConfig contract) + rufinoConfig.ts + migration supplier_exports
# + backfill validation_lists.value_es (motif_sav + bon_type). Test guard
# hardcoded FR36 (grep MARTINEZ/RUFINO dans builder → 0). SheetJS only. 14 AC.
# 5.2 endpoint POST /api/exports/supplier + UI ExportSupplierModal + historique.
# DÉCISION ARCHITECTURALE MAJEURE : Vercel cap 12/12 atteint post-Epic 4 →
# router consolidé /api/pilotage.ts (absorbe exports + reports 5.3 + admin 5.5)
# + consolidation self-service/upload-session|upload-complete dans draft.ts
# (router op-based) → libère 1 slot. Settings onedrive.exports_folder_root
# placeholder fail-closed. Bench p95 < 3s (AC-2.5.1). 17 AC.
# 5.3 reporting dashboard : 4 endpoints (cost-timeline N-1, top-products 90j,
# delay-distribution p50/p90, top-reasons-suppliers) agrégés SQL-side
# (generate_series gap-fill, percentile_cont PG). DashboardView 4 cards +
# chart.js 4 / vue-chartjs 5 (+~70 KB gzip chunk séparé). Migration indexes
# optimisation (idx sav received_at+status, sav_lines motif partiel, etc.).
# Bench p95 < 2s (AC-2.5.3). 16 AC.
# 5.4 export CSV ad hoc : GET /api/reports/export-csv réutilise
# listSavQuerySchema Story 3.2 (filtres consistants). CSV UTF-8 BOM + ';'
# (Excel FR) + virgule décimale + CRLF. Seuil 5000 lignes → SWITCH_TO_XLSX
# warning JSON + 50000 hard limit. 13 colonnes fixes (ref, date, client,
# groupe, totaux, motifs, fournisseurs agrégés string_agg). 13 AC.
# 5.5 cron alertes seuil + admin settings : migration settings.threshold_alert
# (count=5,days=7,dedup_hours=24 défaut) + table threshold_alert_sent (audit
# dédup append-only). Runner threshold-alerts.ts étend dispatcher existant
# (pas de nouveau slot cron Vercel Hobby 2/jour). Email enqueue outbox
# kind='threshold_alert' (delivery activée Epic 6.6 — dépendance documentée).
# Admin UI SettingsAdminView onglet Seuils + PATCH /api/admin/settings/
# threshold_alert versionné. 18 AC.
# 5.6 validation empirique FR36 : martinezConfig.ts + supplier-configs.ts
# map typée + GET /api/exports/supplier/config-list (fetch dynamique UI).
# ZERO modif supplierExportBuilder.ts (test guard Story 5.1 re-vérifié CI).
# Case study doc architecture-client.md. 14 AC.
# Ordre dev recommandé : 5.1 (foundation) → 5.2 (consolidation Vercel critique)
# → 5.3 + 5.4 en parallèle (endpoints reporting indépendants) → 5.5 (cron+admin)
# → 5.6 (validation FR36). Epic 5 passera in-progress à la 1re story qui démarre.
# 2026-04-24 : Story 4.6 done après CR adversarial 3 couches (Blind+Edge+Auditor).
# 15 patches appliqués (abort-on-first-error runWithConcurrency, cleanup scoped
# runTag UUID, assertion range [1..count] stricte, infra vs assertion error,
# word-boundary regex hostname prod, cap count/errors, exit code propagé sur
# cleanup/report failure, threader counts dans report, doc drift W31→W38).
# 1 defer W39 (checklists Epic 4 final + cutover Epic 7). 602/602 Vitest
# (+32 helpers load-test), typecheck strict 0, lint 0, build 459.16 KB stable.
# Task 4 (run réel 10k RPC contre DB préview Supabase dédiée) différé manuel
# pré-cutover Epic 7 — bloque déploiement prod V1 mais pas merge Epic 4.
# → Epic 4 complet 6/6 stories (4.0, 4.0b, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6 done).
# 2026-04-24 : Story 4.5 done après CR adversarial 3 couches (Blind+Edge+Auditor).
# 15 patches P1-P15 appliqués (rate-limit canonical key, migration valid_from
# past date, UPDATE WHERE pdf_web_url IS NULL anti-orphelin, path-traversal
# defense-in-depth folder_root, inject deps ensureFolderExists, is_group_manager
# dérivé discount_cents > 0, guards issued_at/NaN/totals, whitelist failure_kind,
# whitespace-only prénom, log warn issued_at corrompu, idx React key, doc
# deviation bigint vs number). 4 defers W34-W37 (smart retry classification,
# refresh token MSAL 401, @vercel/functions prod install, partial unique index
# settings). 20 dismissed. 570/570 Vitest (+2 CR tests P1/P3), typecheck 0,
# lint:business 0, build 459.16 KB stable.
# 2026-04-24 : Story 4.5 implémentée → review. @react-pdf/renderer v4.5.1 pur
# JS (sans Chromium) + composant CreditNotePdf.ts (charte orange #F57C00,
# A4, 9 col tableau, totaux HT/Remise/TVA/TTC). Pipeline async generate-
# credit-note-pdf.ts (idempotent, retry ×3 OneDrive, fail-closed settings
# placeholder, logs PDF_GENERATED|PDF_UPLOAD_RETRY|PDF_RENDER_FAILED).
# Wait-until wrapper (@vercel/functions optionnel, fallback void catch).
# Endpoint regenerate POST /api/credit-notes/:number/regenerate-pdf
# (synchrone, idempotent 409 PDF_ALREADY_GENERATED, rate-limit 1/min).
# Redirect étendu avec cas PDF_GENERATION_STALE (≥5 min). Migration
# 20260428120000 settings company.* (9 clés placeholder + pdf_folder_root).
# Bench script 50 rendus p50/p95/p99 (manuel, pré-merge). 49 nouveaux
# tests (568/568 Vitest +49), typecheck 0, lint:business 0, build 459.16
# KB stable. Déviation documentée : .ts + React.createElement (pas .tsx
# — tsconfig Vue jsxImportSource) + shim react local (pas @types/react).
# AC #8.5 preview visuelle bloqué par placeholder settings → levé au
# cutover Epic 7.
# 2026-04-24 : Story 4.4 done après CR adversarial 3 couches (Blind+Edge+Auditor).
# 24 findings bruts → triage 10 patches (2 High P1/P2, 5 Medium P3-P7, 3 Low
# P8-P10) + 1 defer (W33 clock drift settings cross-4.3/4.4) + 10 dismissed.
# Patches : P1 cap :number 15 digits (MAX_SAFE_INTEGER), P2 regex format
# \d{5,} (lpad ne tronque pas ≥100k), P3 settings error → 500 bloquant
# (no silent financial fallback), P4 allowlist https:// sur pdf_web_url
# (defense-in-depth open redirect), P5 gate order credit_note existant
# AVANT status check, P6 log warn group_manager + sav.group_id null,
# P7 SQL test 2 RPC back-to-back sav_id → last_number +1, P8 body Array
# rejeté 400 INVALID_BODY, P9 normalizeMember log warn array>1 + 500 si
# null, P10 race 23505 re-SELECT pour inclure number_formatted. Vitest
# 519/519 (+10 CR), typecheck 0, lint:business 0, build 459.16 KB stable.
# 2026-04-24 : Story 4.4 émission atomique → review. Migration UNIQUE(sav_id)
# + handler emit-handler.ts (24 tests Vitest AC #1-7,#10) + handler pdf-
# redirect-handler.ts (8 tests AC #8) + stub generate-pdf-async.ts (Story
# 4.5 override) + dispatcher dédié api/credit-notes.ts (12/12 Vercel Hobby
# functions, plafond atteint) + rewrite vercel.json /api/sav/:id/credit-
# notes + /api/credit-notes/:number/pdf + test SQL issue_credit_number_
# emit.test.sql (3 tests AC #11) + docs api-contracts-vercel.md sections
# émission + re-download. Vitest 509/509 (+59 vs baseline), typecheck 0,
# lint:business 0, build 459.16 KB stable. Stub PDF documenté dégradé
# (Story 4.5 remplacera par @react-pdf + upload OneDrive).
# 2026-04-27 : Stories 4.3 / 4.4 / 4.5 / 4.6 créées (ready-for-dev) — fin
# contextualisation Epic 4. 4.3 preview live (composable useSavLinePreview +
# extension detail-handler settings_snapshot + bloc UI Aperçu avoir + remise
# responsable 4% conditionnelle, 11 AC). 4.4 émission atomique POST
# /api/sav/:id/credit-notes (gate statut + lignes ok + 1 SAV=1 avoir,
# migration UNIQUE(sav_id), totaux via moteur 4.2, RPC 4.1, async PDF
# enqueue via waitUntil, endpoint re-download PDF 302/202, 12 AC).
# 4.5 PDF Fruitstock (@react-pdf/renderer v4.5.1 déjà présent, template
# charte orange + mentions légales + multi-taux TVA V1 simple, settings
# company.* + onedrive.pdf_folder_root, upload OneDrive retry x3, async via
# waitUntil dans même lambda émission, endpoint regenerate stale recovery,
# bench p95 < 2s target, 12 AC). 4.6 load test 10k émissions (script
# standalone Node + tsx, garde-fou anti-prod 3 niveaux, assertions zéro
# collision/zéro trou/séquence-in-sync, cleanup finally, runbook README,
# pas de CI V1 — manuel pré-merge + pré-cutover, 10 AC). Ordre dev
# recommandé : 4.3 + 4.5 en parallèle, puis 4.4 (dépend 4.5 interface),
# puis 4.6 validation empirique.
# 2026-04-25 : Story 4.2 done après CR adversarial 3 couches (Blind+Edge+Auditor).
# 1 BLOCKER résolu, 14 patches P1-P14 appliqués, 2 decisions tranchées (D1 NULL
# qty_invoiced → to_calculate cohérent flow double-webhook Make.com ; D2 pas
# de bump sav.version — verrou social via assignation opérateur). 13 defers
# W16-W28 ajoutés dans deferred-work.md, 18 dismissed. Migration CR patches
# 20260426130000 ajoutée (D1 + P3 trigger immutability snapshot + P4
# recompute FOR UPDATE + IS DISTINCT FROM + P10 validation_messages sync).
# Tests : 81 TS (coverage 91.74%) + 25 SQL trigger + 5 SQL miroir fixture +
# 10 SQL sav_lines + 7 SQL duplicate + 5 autres RPC = 133 assertions. Vitest
# 450/450, typecheck 0, lint:business 0, build 459 KB stable. 7/8 tests SQL
# OK local (transition_sav_status superuser pré-existant passe CI).
# 2026-04-25 : Story 4.2 créée (ready-for-dev). Moteur calcul avoir TS pur
# (creditCalculation + pieceKgConversion + vatRemise + settingsResolver) +
# triggers PG miroirs (compute_sav_line_credit BEFORE, recompute_sav_total
# AFTER) + fixture partagée ≥ 20 cas synthétiques PRD-dérivés + script gen
# SQL miroir 5 cas + step CI check-fixture-sql-sync + ESLint no-io-in-business
# + CHECK credit_coefficient ∈ [0,1]. 18 AC, 11 tasks, estimation 2-3j dev.
# Défense-en-profondeur 5 couches (UI/Zod/CHECK/trigger/moteur TS). Débloque
# 3.6b carry-over + 4.3 UI preview + 4.4 émission atomique.
# 2026-04-24 : Story 4.1 done après CR adversarial 3 couches (Blind+Edge+Auditor).
# 0 BLOCKER, 2 decisions tranchées (D1 RLS operator wildcard accepté V1 durcissement
# Epic 6, D2 séquence mondiale conservée + trigger immutability pour protéger
# number_formatted post-UPDATE), 3 patches appliqués (P1 trigger immutability
# 10 colonnes gelées, P2 CHECK last_number>=0, P3 normalisation bon_type
# upper+trim), 6 defers (W10-W15 dans deferred-work.md), 5 dismissed.
# Tests : 14/14 (11 initiaux + 3 CR patches). Migration 20260425140000 CR patches
# ajoutée. 369/369 Vitest, typecheck 0, build 459 KB stable.
# 2026-04-24 : Story 4.1 créée (migration credit_notes + credit_number_sequence
# + RPC issue_credit_number atomique + test SQL concurrence). 0 code TS,
# signature RPC étendue à 7 args (divergence epics documentée). 2 migrations
# SQL + 1 test SQL + 1 README update. Ready-for-dev.
# 2026-04-23 late evening : Story 4.0b créée (dette tests SQL RPC Epic 3
# + wiring CI). Pattern 4.0 à réutiliser, 0 impact code TS. 5 fichiers SQL
# + 1 step CI + 1 update tracker README. Ready-for-dev.
# 2026-04-23 CR adversarial Story 4.0 : 3 decisions (D1 défer, D2+D3 patchés),
# 4 patches (P1 unique_violation handler, P2 mock unit_mismatch, P3 Test 9b,
# P4 credit_coefficient type), 4 défers, ~22 dismissed. Story 4.0 → done.
# 2026-04-23 late : Story 4.0 dette créée (alignement sav_lines PRD-target
# + enum validation_status CHECK strict). Epic 4 passe in-progress.
# 2026-04-23 evening : Story 4.0 implémentée → review. 369/369 tests Vitest
# (+5 vs baseline 364), typecheck 0, build 459 KB (stable). Migrations
# 20260424120000 (schéma) + 20260424130000 (RPCs) + test SQL RPC
# sav_lines_prd_target.test.sql (9 scénarios).
# Story 2.1 : done (864 produits importés, 18 catégories séparateurs,
# 10/10 tests RLS OK, 211/211 tests Vitest, typecheck 0).
# Epic 1 : 7/7 stories done après code review adversariale (rapport :
# _bmad-output/implementation-artifacts/epic-1-review-findings.md).
# Patches P1-P16 + décisions D1-D4 appliqués. 208/208 tests, typecheck 0.
# Epic 2 : 4/4 stories done après code review adversarial (rapport :
# _bmad-output/implementation-artifacts/epic-2-review-findings.md).
# F1-F8 patches appliqués, 267/267 tests, typecheck 0, build OK.
# Epic 3 : 7/7 stories contextées → ready-for-dev (2026-04-22). Epic in-progress.
# Epic 3 : CR adversarial 3 couches effectué 2026-04-23 (rapport :
# _bmad-output/implementation-artifacts/epic-3-review-findings.md). 95
# findings uniques. Patches P0 sécurité appliqués : F5 (cursor injection),
# F8 (q escape), F50 (ACTOR_NOT_FOUND sur 5 RPCs), F52 (validation_status
# retiré du wire + whitelist RPC), F36/F37 (PII leak), D6 (SAV_LOCKED édition
# ligne terminale). Decisions D1-D6 tranchées : D1 = Option C (split V1/V1.1),
# D2/D3 reportées Epic 4 (moteur calcul), D4 code FE orphelin accepté V1
# (cleanup Epic 7), D5 `admin` est role pas type (aucun patch).
# Epic 3 : tous patches HIGH/MEDIUM/LOW appliqués suite à "on va tout faire
# dans l'ordre" (2026-04-23 soirée) : F2 (unicode CHECK), F7/F9/F10/F11/F14/
# F16/F17/F19/F20/F97 (Story 3.2), F21/F24/F25/F26/F27/F28/F29 (Story 3.3),
# F33/F34/F38/F39/F41/F42/F43/F44/F45/F47/F48/F49 (Story 3.4), F51/F57/F58/
# F59/F61/F62 (Story 3.5). Migration 20260423120000_epic_3_cr_security_patches.sql
# regroupe les durcissements SQL. 364/364 tests Vitest (+14 vs baseline),
# typecheck 0, build 459 KB. Stories 3.1-3.5 = done, 3.6/3.7 = done V1 minimal
# avec carry-over 3.6b/3.7b en backlog.
project: sav-monorepo
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ──────────────────────────────────────────────────────────────
  # Epic 1: Accès authentifié & fondations plateforme
  # ──────────────────────────────────────────────────────────────
  epic-1: done
  1-1-setup-typescript-strict-migration-dependances: done
  1-2-migration-bdd-initiale-identites-audit-auth-infra: done
  1-3-middleware-serverless-unifie: done
  1-4-auth-msal-sso-operateur-admin: done
  1-5-auth-magic-link-adherent-et-responsable: done
  1-6-audit-trail-et-journalisation-transverse: done
  1-7-infrastructure-jobs-cron-ci-cd-healthcheck: done
  epic-1-retrospective: optional

  # ──────────────────────────────────────────────────────────────
  # Epic 2: Capture client fiable avec persistance & brouillon
  # ──────────────────────────────────────────────────────────────
  epic-2: done
  2-1-migration-tables-sav-catalogue-import-initial: done
  2-2-endpoint-webhook-capture-avec-signature-hmac: done
  2-3-brouillon-formulaire-cote-serveur-auto-save: done
  2-4-integration-onedrive-dans-le-flow-capture: done
  epic-2-retrospective: optional

  # ──────────────────────────────────────────────────────────────
  # Epic 3: Traitement opérationnel des SAV en back-office
  # ──────────────────────────────────────────────────────────────
  epic-3: done
  3-1-migration-commentaires-sav: done
  3-2-endpoint-liste-sav-filtres-recherche-pagination-cursor: done
  3-3-vue-liste-sav-en-back-office: done
  3-4-vue-detail-sav-en-back-office: done
  3-5-transitions-de-statut-assignation-verrou-optimiste: done
  3-6-edition-lignes-sav-avec-validations-bloquantes: done
  3-7-tags-commentaires-duplication-fichiers-additionnels: done
  # Carry-over créés (backlog) :
  3-6b-triggers-compute-sav-line-credit-et-ui-edition-ligne: done  # 2026-04-24 DS + CR adversarial 3 couches (Blind+Edge+Auditor). 13/13 AC ✅. 7 patches CR appliqués (P1 MISSING_FIELD, P2 locale FR virgule, P3 reset-null update_sav_line, P4 refreshSafe hors try/catch, P5 confirmFn doc, P7 cleanup editDraft, P9 FORBIDDEN_FIELD F52). Migration 20260430120000 CR patches. 628/628 Vitest, typecheck 0, lint 0, build 459.64 KB.
  3-7b-ui-tags-compose-duplicate-upload-operateur: backlog             # dépend Epic 6 self-service
  epic-3-retrospective: done

  # ──────────────────────────────────────────────────────────────
  # Epic 4: Moteur comptable fidèle (calculs, avoirs, bons SAV PDF)
  # ──────────────────────────────────────────────────────────────
  epic-4: done
  # Story 4.0 dette Epic 4 prep : alignement schéma sav_lines PRD-target (D2)
  # + enum validation_status CHECK strict (D3). Pré-requis Story 4.2 moteur calcul.
  4-0-dette-schema-sav-lines-prd-target: done
  # Story 4.0b dette Epic 4 prep #2 : convention + 5 tests SQL RPC Epic 3
  # (transition_sav_status, assign_sav, update_sav_tags, duplicate_sav,
  # capture_sav_from_webhook) + wiring CI migrations-check.
  4-0b-dette-tests-sql-rpc-epic-3: done
  4-1-migration-avoirs-sequence-transactionnelle-rpc: done
  4-2-moteur-calculs-metier-typescript-triggers-miroirs-fixture-excel: done
  4-3-integration-moteur-vue-detail-preview-live: done
  4-4-emission-atomique-n-avoir-bon-sav: done
  4-5-template-pdf-charte-fruitstock-generation-serverless: done
  4-6-test-de-charge-sequence-d-avoir: done
  epic-4-retrospective: optional

  # ──────────────────────────────────────────────────────────────
  # Epic 5: Pilotage — exports fournisseurs + reporting + alertes
  # ──────────────────────────────────────────────────────────────
  epic-5: in-progress  # démarré 2026-04-24 avec Story 5.1
  5-1-architecture-export-generique-config-rufino-migration: done
  5-2-endpoint-export-fournisseur-ui-back-office: done
  5-3-endpoints-reporting-dashboard-vue: done
  5-4-export-csv-reporting-ad-hoc: done  # DS+CR complets 2026-04-27 (3 patches initiaux). Second-pass CR adversarial 3 couches 2026-04-28 → 14 patches additionnels appliqués (P1 CSV-injection, P2 formatEurFr round, P3 format ambigu, P4 member nullable, P5 revoke 30s, P6 q-routing anchored, P7 aborted statut discriminé, P8 hashFilters canon, P9 downloading counter, P10 truncation log warn, P11 supplier trim, P12 Excel cap 32k, P13 EMPTY_RESULT, P14 toast auto-dismiss) + 3 defers W53-W55. Suite 866/866 verte, bundle 460.44 KB ≡ baseline.
  5-5-job-cron-alertes-seuil-produit-config-admin: done  # 2026-04-28 CR adversarial 3 couches : 2 décisions tranchées + 29 patches + 11 defers W57-W67. Migration hardening 20260507140000 (S1 GRANT/REVOKE, R10 soft-delete filter, D6 bigint, D8 immutable trigger, RPC update_settings_threshold_alert + RPC enqueue_threshold_alert transactionnelle). Runner refactor try/catch per-product + normalize emails + NaN/SafeInteger guards + .gte boundary + CRLF strip + APP_BASE_URL fail-fast prod + order received_at + alerts_failed. PATCH handler via RPC + 23505→409 CONCURRENT_PATCH + rate-limit 10/15min/op + Zod .strict + notes control-chars refine. UI selectTab preserve null + formHydrated/formIsValid + onBeforeUnmount toast. AC #5 amendée (logo image différé V2). 912/912 Vitest (+7), typecheck 0, lint:business 0, build 460.72 KB stable. E2E AC #15 complet 2026-04-28 (steps 1-4+6 SQL/curl, step 5 UI via Chrome DevTools MCP) : W67 résolu, voir 5-5-validation-e2e.md + screenshots 5-5-step5-0{1,2,3}-*.png. Migration Story 5.8 `20260506130000_operators_magic_link` appliquée sur préview au passage (manquait).
  5-6-ajout-d-un-deuxieme-fournisseur-validation-architecture: done  # 2026-04-28 DS + CR adversarial 3 couches (Blind+Edge+Auditor via Sonnet 4.6, lecture indépendante d'Opus qui a fait le DS). 1 HIGH + 4 MEDIUM + 14 LOW = 19 patches P1-P19 appliqués + 4 defers W68-W71 + ~20 dismissed. Highlights : P1 toast manquant ExportHistoryView (parité AC#6/#9), P2 double-fetch loadHistory eliminator (flag pendingExplicitHistoryLoad + nextTick), P3 race hydrateFromQuery+loadConfigList, P4 deep-link ?supplier=TOTO guard, P5 nouveaux specs ExportHistoryView + exports-config-list-handler, P6 FALLBACK_SUPPLIERS factorisé dans composable, P7 types alignés (commentaires DOIT-rester-en-sync), P9 validation per-entry isSupplierConfigEntry, P11 Math.trunc PESO_KG explicite, P12 bench supplier-guard fail-fast, P14 total_amount_cents test (lock divergence XLSX), P19 _registry typé exposé directement. 937/937 Vitest (+13 vs baseline 924), typecheck 0, lint:business 0, build 460.72 KB ≡ baseline, vercel 11/12 functions, guard FR36 vert.
  5-7-cutover-make-pennylane-emails: done  # 2026-04-28 CR complet (bmad-code-review). DS+CR. Cutover Make → app livré : migration sav_submit_tokens + Pennylane v2 client (`pennylane.ts` + `findInvoiceByNumber`) + endpoint `/api/invoices/lookup` (multiplexed `/api/invoices.ts` 12/12 slot) + endpoint anonyme `/api/self-service/submit-token` (capture-token JWT scope=sav-submit single-use 5min) + auth polymorphique `webhooks/capture.ts` (HMAC|capture-token, priorité HMAC + warn dual-auth) + emails fire-and-forget post-INSERT (Promise.allSettled, NODE_ENV=test await) + smtp.ts refactor multi-compte (`noreply` Story 1.5/5.8 + `sav` 5.7) + templates `sav-capture-templates.ts` (interne `replyTo: customer.email` + accusé tutoiement charte #ea7500) + frontend cutover Home.vue regex `F-YYYY-NNNNN` + useApiClient GET lookup unwrap `{invoice}` + WebhookItemsList transformation captureWebhookSchema + InvoiceDetails fallback `customer.id` v2. Schema unit enum étendu `kg|piece|liter|g`. 1013/1013 Vitest (+89 vs baseline 924), typecheck 0, lint:business 0, build 463.44 KB (+0.59% sous seuil ±2%). Runbook `docs/cutover-make-runbook.md` créé (prérequis Antho 9 env vars + phase double-écriture + 5-step cutover + rollback + checklist J+1/J+7/J+30). 8 deferred W72-W79 (suppression Make J+30, suppression VITE_WEBHOOK_URL* J+30, migration email_outbox Epic 6, validation D1/D2 curl preview, comm adhérents D3, reCAPTCHA V1.5, purge cron sav_submit_tokens, nettoyage htmlTable Vue). À faire avant cutover : génération PENNYLANE_API_KEY + provisioning Vercel + validation curl preview + décision PM comm adhérents. via bmad-create-story + Q1-Q5 clarifications résolues avec Antho. ACs 15 (AC#1, #2, #3, #4a smtp multi-compte, #4, #5, #6, #7, #8, #9, #10, #11 tests, #12, #13, #14), tâches 13, 35+ tests spécifiés. Décisions PM tranchées : (1) Pennylane v2 LIST + filter invoice_number:eq: uniquement, pas de fallback v1 (deadline v1 1er juillet 2026 trop court), avec UX change Home.vue input = numéro F-YYYY-NNNNN au lieu du « 14 chars » legacy ; (2) 2 comptes SMTP Infomaniak (noreply@ Story 1.5/5.8 conservé + sav@ nouveau dédié SAV emails) → smtp.ts refactoré multi-account ; (3) Capture-token JWT scope 'sav-submit' single-use via nouvelle table sav_submit_tokens (pas extension polymorphique magic_link_tokens) ; (4) Auth /api/webhooks/capture polymorphique HMAC|capture-token ; (5) Emails fire-and-forget post-201 (best-effort, queue email_outbox différée Epic 6). Audit Make scenarios 3197846 + 3203836 ingéré, blueprint emails Make snapshotté. Volumétrie pic confirmée 10 SAV/jour → rate-limit 5/min/IP largement OK. Reste D1-D3 à lever en DS (forme exacte payload v2 list, URL-encoding filter `:`, message UX adhérents). CR 2026-04-28 (bmad-code-review) : 52 findings (3 reviewers parallèles), 14 patches appliqués + 20 deferred (W80-W99) + 16 dismissed. Décisions tranchées avec Antho : (1) suppression branche HMAC `MAKE_WEBHOOK_HMAC_SECRET` du backend post-cutover (Make tué J+0, rollback côté front), (2) emails parallèles Promise.allSettled accepté (spec amendé). Patches critiques : P1 `waitUntilOrVoid` (sinon 0 emails en prod), P2 retry désactivé sur submitSavWebhook (anti-duplication SAV), P3 sanitize body Pennylane (PII), P4 valider scheme `dossierSavUrl` (XSS), P5 Cache-Control errors submit-token. Mediums : P6 warn multi-match Pennylane, P7 fail-fast SMTP_SAV_PASSWORD, P8 test PII assertion, P9 test CE-06 fix LES 2 emails throw, P10 typ JWT, P11 parseOp discriminé, P12 clamp subject 200ch, P13 retry désactivé lookup, P14 reject submit si invoice manquant. 1008/1008 tests verts (-5 vs DS car CA-01/CA-02/CA-08 + 2 HMAC capture.spec.ts retirés, +1 CA-09 ajouté), typecheck 0, lint app 0 (2 erreurs e2e/ pré-existantes hors scope), build 463.14 KB (-0.06% vs DS).
  5-8-refonte-auth-operateurs-magic-link: done  # DS+CR complets 2026-04-27, magic link sur operators (réutilise infra adhérents). 5 patches CR appliqués + 11 items deferred. Suite 795/795 verte.
  epic-5-retrospective: optional

  # ──────────────────────────────────────────────────────────────
  # Epic 6: Espace self-service adhérent + responsable + notifications
  # ──────────────────────────────────────────────────────────────
  epic-6: in-progress  # contextualisation 7 stories ready-for-dev 2026-04-29
  6-1-migration-email-outbox-preferences-notifications: done  # 2026-04-29 DS+CR yolo — migration 20260509120000_email_outbox_enrichment.sql (9 colonnes, 6 CHECKs, 3 index, 1 trigger combiné `tg_email_outbox_maintain` clock_timestamp + sync retry_count + search_path lockdown W2/W10/W17, 1 backfill, COMMENT). Whitelist `kind` étendue 9 valeurs (incl. `sav_received` rétro-compat). `members.notification_prefs` CHECK schéma JSONB + idx_members_weekly_recap_optin + backfill. Test SQL 11/11 cas pass. Vitest 1008/1008 zéro régression. CR adversarial PASS (1 HIGH B1 search_path appliqué, 0 blocker). Trace 100% coverage 11/11 ACs gate PASS. Migration appliquée sur preview viwgyrqpyryagzgvnfoi.
  6-2-landing-magic-link-liste-sav-adherent: done  # 2026-04-29 DS+CR+Trace yolo — landing /monespace/auth + liste /monespace + guard requiresAuth='magic-link'. 3 ops ajoutées au router self-service (me/sav-list/sav-detail) + 3 rewrites Vercel. 39 nouveaux tests Vitest verts (1047/1047 zero régression) + 4 cas SQL RLS. Typecheck 0, lint:business 0, build 464.26 KB (cap 470 OK). Slots Vercel 12/12 inchangés. CR adversarial PASS (0 Critical/High, 4 Medium / 3 Low non bloquants). Trace 96.7% coverage 14/15 full + AC #3 NFR-P6 partial (perf E2E manuel acceptable per story) gate PASS.
  6-3-detail-sav-adherent-commentaires-bidirectionnels-fichiers: done  # 2026-04-29 DS+CR+Trace yolo. 17/17 ACs (16 FULL + 1 PARTIAL acceptable AC15 sous-composants couverts via vue intégrée), gate Trace PASS. Migration `20260509130000_sav_files_uploaded_by.sql` (CHECK XOR doux + FK ON DELETE SET NULL + backfill member_id + 2 idx partiels). Handler enrichi `sav-detail-handler.ts` (privacy-strict : pas d'email/displayName op, pas oneDriveItemId, pas credit_coefficient, motifs FR via `validation_lists.list_code`+`value_es`, anti-énumération 404 single eq.eq.maybeSingle, authorLabel server-side `Vous`/`Membre`/`Équipe Fruitstock`). Nouveau handler `sav-comment-handler.ts` (POST INSERT + email_outbox kind=`sav_comment_added` lookup `operators.email` via `sav.assigned_to` + skip si pas assignee/email + rate-limit composé `member:<sub>:<savId>` 10/min + Zod no-control-chars trim 1-2000). Op `sav-comment` ajoutée à router self-service `draft.ts` + rewrite Vercel `/api/self-service/sav/:id/comments`. Vue `MemberSavDetailView.vue` + composable `useMemberSavDetail.ts` + 5 sous-composants (Summary/Lines/FilesList/CommentsThread/StatusHistory). Pipeline upload 3-temps réutilisé Story 2.4 (savReference branch upload-complete). RLS audit : policies déjà posées Story 2.1/3.1 — DB défense-en-profondeur ✅. CR adversarial 3 couches : 8 findings → 2 HIGH patchés (P1 recipient_email broken outbox CHECK violation silently swallowed → lookup operators.email + skip best-effort si pas assignee, P2 typo column `list_key`→`list_code` motifs raw fallback) + 3 LOW deferred (W100-W102) + 3 dismissed. 66 nouveaux tests Vitest + 3 SQL sécurité. Suite 1108/1108 (+61 vs baseline 1047), typecheck 0, lint:business 0, build 464.43 KB (sous cap 472 KB AC#17). Vercel slots 12/12 préservés. AC#3 NFR-P6 perf < 10s = mesure manuelle pré-merge (acceptable per story).
  6-4-telechargement-pdf-bon-sav-preferences-notifications: done  # 2026-04-29 DS+CR+Trace+Hardening checkpoint. 14/14 ACs FULL post-hardening (init: 11 full / 3 partial / 1 forward-traced AC#10→6.6). Extension polymorphique `pdfRedirectHandler` Story 4.4 (member/operator) avec anti-énumération 404 + rate-limit séparé par bucket (`credit-note-pdf:member` 30/min vs operator 120/min). Nouveau handler `preferences-handler.ts` GET/PATCH `/api/self-service/preferences` + Zod `.strict().refine(keys>0)` + filtre `anonymized_at IS NULL` 404 anti-leak. **W104 RÉSOLU** — Migration `20260509140000_member_prefs_merge_rpc.sql` RPC SECURITY DEFINER `member_prefs_merge(bigint, jsonb)` opérateur SQL `||` natif atomique (élimine race read-modify-write last-writer-wins, prêt 6.7). Extension `meHandler` lookup `members.is_group_manager` + 3 tests P1 directs (membres true/false + operator no-lookup, fix "fake green" pré-CR). Vue `MemberPreferencesView.vue` + composable `useMemberPreferences` (parallel fetch /me + /preferences, toggle weekly_recap masqué si !isGroupManager). Bouton « Télécharger bon SAV » dans `MemberSavDetailView` + état pending. CR adversarial 3 couches : PASS-WITH-PATCHES (0 BLOCKER/CRITICAL, 14 IMPORTANT validés, 6 DEFERRED W103-W108 tracés). Patches mandatory P1 (me-handler tests) + P3 (deferred-work) appliqués. Hardening additionnel post-CR : W104 RPC swap, AC#3 test empirique débordement rate-limit (429), AC#11/#12 tests directs MemberSpaceLayout nav-link (3) + parseOp preferences (2), W108 doc api-contracts-vercel.md section Story 6.4 (envelope `{ data: { notificationPrefs } }` documentée + drift spec/code expliqué). Suite 1140/1140 (+9 vs 1131 baseline), typecheck 0, lint:business 0, build 464.55 KB inchangé (cap 472, marge 7.45 KB). Vercel slots 12/12 inchangés. Decisions D1 (envelope `{data}` vs spec `{notificationPrefs}` direct — code suit pattern self-service dominant ; documenté W108) + D2 (RPC `||` natif post-hardening, plus le merge applicatif initial) + D3 (auto-refresh 30s `credit-note-pdf-pending` déféré W103, hors AC#1).
  6-5-scope-etendu-responsable-de-groupe: done  # 2026-04-29 DS+CR adversarial 3-layer. 13/13 ACs implémentés. Migration `20260509150000_rls_group_manager_scope.sql` (timestamp 140000 occupé → 150000) pose 4 policies nommées explicitement (sav/sav_lines/sav_files/sav_comments) + JWT-claims-based pattern (complète l'existant `app_is_group_manager_of` GUC-based). Helper `requireActiveManager()` Layer 2 (re-check DB `is_group_manager` + `anonymized_at`, fail-closed). 3 handlers étendus : sav-list (scope query + filtres `q`/dates + 403 SCOPE_NOT_AUTHORIZED + 403 SCOPE_REVOKED), sav-detail (query polymorphique `.or()` + `member: {firstName,lastName}` exposé conditionnel + cross-group guard), sav-comment (ownership polymorphique + 2e outbox enqueue `recipient_member_id` propriétaire skip si auteur==owner). Codes erreur `SCOPE_NOT_AUTHORIZED`/`SCOPE_REVOKED` ajoutés à `errors.ts` (HTTP 403). Frontend `MemberSavListView` deux instances composable cohabitent (selfList + groupList lazy mount manager) + TabsBar inline + colonne Adhérent + filtre `q` re-fetch ; `MemberSavDetailView` badge `data-testid="group-sav-badge"` rendu via présence `data.member` (signal serveur). Privacy email triple-enforcement : SELECT_EXPR exclut email + tests assertent `JSON.stringify(body)` !~ /email/ + tests assertent `selectExpr` !~ /email/. 24 nouveaux tests verts (sav-list-6-5 8 + sav-detail-6-5 5 + sav-comment-6-5 5 + manager-check 6 + MemberSavListView S6.5 4) — suite 1168/1168 vert. Test SQL `sav_group_manager_rls.test.sql` 5 cas BEGIN/ROLLBACK rolled-back (à exécuter en CI/préprod via psql). Typecheck 0, lint:business 0, lint Story 6.5 files 0 (prettier auto-fix appliqué), build 464.55 KB < 472 KB cap. Slots Vercel 12/12 inchangés (3 handlers étendus, 0 nouveau endpoint). Décisions notables : (D1) timestamp migration 150000 vs 140000 spec'd, (D2) policies RLS additives nommées explicitement même si fonctionnellement déjà couvert via `app_is_group_manager_of` (alignement spec AC #10 + traçabilité audit), (D3) Layer 2 cost-aware (re-check DB UNIQUEMENT si `accessedAsManager`), (D4) cross-group guard explicite après .or() pour blinder un éventuel mauvais parsing Postgrest, (D5) `useMe` pas extrait en composable partagé (cohérence pattern existant useMemberPreferences). Risques résiduels documentés dans Dev Agent Record pour CR : mock .or() parser permissif, `accessedAsManager=false` quand manager regarde son propre SAV (skip Layer 2 acceptable), ilike posté via jointure Postgrest (orphelins members théoriquement impossibles vu FK NOT NULL).
  6-6-envoi-emails-transactionnels-transitions-nouveau-sav-via-outbox-retry: done  # 2026-04-29 DS+CR adversarial 3-layer + Hardening checkpoint. 12/12 ACs (47/56 sub-items FULL, 4 PARTIAL non-bloquant, 0 NOT-MET, 3 FORWARD-TRACED documentés). Migration `20260510120000_transition_sav_status_template_data.sql` : (a) refresh RPC `transition_sav_status` avec `template_data` JSONB rich (savReference/savId/memberId/firstName/lastName/newStatus/previousStatus/totalAmountCents) ; (b) RPC `enqueue_new_sav_alerts(p_sav_id)` SECURITY DEFINER broadcast 1 row par opérateur actif (`role IN ('admin','sav-operator') AND is_active=true`) ; (c) RPC `mark_outbox_sent(p_id, p_message_id)` atomique (élimine race read-modify-write) ; (d) RPC `mark_outbox_failed(p_id, p_error, p_next_attempt_at, p_definitive)` symétrique ; (e) RPC `claim_outbox_batch(p_limit)` worker-safe FOR UPDATE SKIP LOCKED + colonne `claimed_at` + stale recovery 5min (P0-7 anti double-cron). 6 templates `client/api/_lib/emails/transactional/{_layout,types,render,kinds,sav-in-progress,sav-validated,sav-closed,sav-cancelled,sav-received-operator,sav-comment-added}.ts` (6 templates + helpers fonctions pures testables, charte orange #ea7500). Runner `client/api/_lib/cron-runners/retry-emails.ts` (batch ≤100, p-limit cc=5, Promise.race timeout 10s, backoff `2^attempts*60s` cap 24h MAX_ATTEMPTS=5, opt-out kinds adhérent via `notification_prefs->>'status_updates'`). Webhook `capture.ts` étendu Promise.allSettled enqueue_new_sav_alerts fire-and-forget. Dispatcher cron étendu runRetryEmails après runThresholdAlerts. **Hardening adversarial 3-layer** (Blind/EdgeCase/Acceptance) → 7 P0 + 6 I patches inline + 1 I déferré : P0-1 split index dédup (`_no_operator` + `_per_operator`) anti-blocage broadcast multi-op, P0-2 verify SELECT post-markErr smtp_message_id (anti-double-send), P0-3 NOT EXISTS étendu `IN ('pending','sent')` + window 24h anti-replay + test SQL cas (d), P0-4 (covered par P0-1), P0-5 stripCrlf étendu U+0085/U+2028/U+2029 (anti header-injection), P0-6 attempts NULL/undefined/-1 guard Number.isInteger (anti NaN cascade), P0-7 claim_outbox_batch worker-safe (FOR UPDATE SKIP LOCKED + claimed_at) ; I1 docstring backoff aligné, I2 member anonymisé→cancelled `member_not_found`, I3 MEMBER_KINDS centralisé `kinds.ts`, I4 isSafeHttpUrl scheme http(s) defense-in-depth, I8 migration rollback section, I9 sav-comment-added text strip tags ; I5 SMTP transporter.close()-on-timeout déféré V1 (deferred-work.md). Suite Vitest 1272/1272 (+104 vs 1168 baseline 6.5 — incl. P0-2/P0-6/I2 hardening tests), test SQL cas a/b/c/c-bis/d verts, typecheck 0, lint:business 0, build 464.55 KB < 472 KB cap, Vercel slots 12/12 inchangés. Trace coverage matrix `_bmad-output/test-artifacts/trace-matrix-6-6-*.md` gate PASS. Drifts FORWARD-TRACED ratifiés Layer 3 : (D1) AC #2 dédup `WHERE NOT EXISTS (sav_id, kind, recipient_operator_id)` au lieu de ON CONFLICT (couvre strictement plus, dicté par index partiels P0-1) ; (D2) AC #5 6 templates au lieu de 8 (DS Q1 résolu pré-DS, contradiction interne spec AC#5/Tasks line 132) ; (D3) AC #9 sémantique `status='pending'` jusqu'à attempts=5 puis 'failed' définitif (sémantique métier correcte). Décisions DS notables : (DS-Q2) p-limit ^3.1.0 vs maison, (DS-Q3) Promise.race+setTimeout vs AbortController (nodemailer ne supporte pas), (DS-Q4) sav-received-operator pas de unsubscribe (kind opérateur), (DS-Q5) last_error err.message brut audit complet, (DS-Q6) RPCs inline migration unique (atomicité rollback). Risques résiduels post-hardening : 0 HIGH/MEDIUM, 2 LOW déferrés (I5 SMTP leak V1-acceptable + Layer1-NIT messageId logging RGPD audit post-merge).
  6-7-recap-hebdomadaire-responsable-opt-in: done  # 2026-04-29 DS+CR adversarial 3-layer + Hardening + Trace checkpoint. 10/10 ACs FULL post-hardening (23/23 sub-items). Runner `weekly-recap.ts` (guard vendredi UTC `getUTCDay()===5` + override env `WEEKLY_RECAP_BYPASS_FRIDAY` **gardé NODE_ENV!=production** post-hardening M1, SELECT managers opt-in via idx_members_weekly_recap_optin, SELECT recap fenêtre ISO week `date_trunc('week', now())` post-hardening H1, INSERT outbox avec template_data camelCase, try/catch per-row, dédup absorbe SQLSTATE 23505 + compteur `skipped_dedup` dédié post-hardening H3). Template `weekly-recap.ts` (tableau HTML inline-styled charte orange #ea7500 + 1 lien direct par ligne `/monespace/sav/{id}` + footer désinscription, text fallback, escapeHtml strict, stripCrlf subject). Migration `20260510140000_email_outbox_weekly_recap_dedup.sql` (UNIQUE INDEX partiel `idx_email_outbox_weekly_recap_unique` sur `(recipient_member_id, date_trunc('week', created_at))` WHERE kind='weekly_recap'). Dispatcher cron étendu `runWeeklyRecap` après `runRetryEmails`. `retry-emails.ts` étendu : opt-out check `weekly_recap` utilise `notification_prefs.weekly_recap` (opt-in default false) au lieu de `status_updates`. **CR adversarial 3-layer** (Blind Hunter / Edge Case Hunter / Acceptance Auditor) → REQUEST CHANGES initial avec 3 BLOCKERS + 3 HIGH + 4 MEDIUM/LOW : B1 leak RGPD (query SAV recap n'excluait pas members.anonymized_at IS NULL → PII re-divulgation via récap), B2 PostgREST embed ambigu (relation `members` répétée 2× alias différents → PGRST201 ambiguous embed prod ; mock plat masquait le bug), B3 test opt-out runtime weekly_recap manquant retry-emails.spec.ts (Task 5 Sub-3 déféré DS), H1 mismatch fenêtre 7j JS vs date_trunc('week') dédup, H3 `skipped_dedup` counter manquant observabilité, M1 env var bypass non gardée prod. **Hardening pass complet** (Round 1) : B1 fix `.is('member.anonymized_at', null)` filtre embed inner-join + cas (k) test ; B2 single embed `member:members!inner(first_name, last_name, anonymized_at)` + drop dual-shape mock fallback ; B3 2 cas (X opt-out → cancelled member_opt_out, Y opt-in avec status_updates=false isolation pref → SMTP send) ; H1 helper `startOfIsoWeekUtc()` periodStart=lundi 00:00 UTC ; H3 field `skipped_dedup` au type WeeklyRecapResult + cas (h-bis) test ; M1 throw `WEEKLY_RECAP_BYPASS_FRIDAY not allowed in production` + cas (l) test. Suite Vitest **1292/1292** (+5 vs 1287 GREEN-phase, +20 vs 1272 baseline 6.6 — 12 cas runner [a-l + h-bis] + 5 cas template + 2 cas retry-emails opt-out + 4 cas SQL dédup), typecheck 0, lint:business 0, build 464.55 KB < 475 KB cap (marge 10.45 KB). Slots Vercel 12/12 inchangés (0 nouveau endpoint, runner intégré dispatcher daily). Trace coverage matrix `_bmad-output/test-artifacts/trace-matrix-6-7-recap-hebdomadaire-responsable-opt-in.md` **gate PASS** 100% (10/10 ACs FULL, 23/23 sub-items, couverture supérieure minimum AC#9 12+5+4 vs spec 10+5+2). Décisions DS notables : (D1) Dédup absorbée comme observabilité dédiée `skipped_dedup` après hardening H3 (vs `errors=0/enqueued=0` initial DS) ; (D2) Embed PostgREST single-relation `member:members!inner(...)` post-hardening B2 (vs dual-alias initial qui masquait l'ambiguïté en mock) ; (D3) Fenêtre ISO week `date_trunc('week', now())` post-hardening H1 (vs `now() - interval '7 days'` glissant initial DS — alignement avec index dédup) ; (D4) Env var bypass NODE_ENV-guarded post-hardening M1. Risques résiduels post-hardening : 0 HIGH/MEDIUM bloquants ; déferrés non-bloquants : Task 7 runbook docs `docs/email-outbox-runbook.md` à compléter avant 1er prod-Friday cron tick, Task 6 Sub-5 E2E manuel preview via `WEEKLY_RECAP_BYPASS_FRIDAY=true` (gardé prod par M1), test SQL dédup attendu apply migration preview psql. **Epic 6 COMPLET** : 7/7 stories done (6.1→6.7).
  epic-6-retrospective: optional

  # ──────────────────────────────────────────────────────────────
  # Epic 7: Administration, RGPD, intégration ERP, cutover prod
  # ──────────────────────────────────────────────────────────────
  epic-7: backlog
  7-1-migration-erp-push-queue-module-push-builder: backlog
  7-2-push-erp-au-passage-cloture-retry-alerte: backlog
  7-3-ecrans-admin-operateurs-catalogue-listes-validation: backlog
  7-4-ecran-admin-settings-versionnes: backlog
  7-5-audit-trail-filtrable-file-erp-consultable: backlog
  7-6-admin-rgpd-export-json-signe-anonymisation: backlog
  7-7-cutover-scripte-runbooks-dpia: backlog
  epic-7-retrospective: optional
