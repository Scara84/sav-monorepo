# Travaux différés — sav-monorepo

## Deferred from: code review of 4-0-dette-schema-sav-lines-prd-target (2026-04-23)

- **D1 — COALESCE empêche le null-clearing des champs nullable dans `update_sav_line`** : `unit_invoiced`, `qty_invoiced`, `credit_coefficient_label`, `piece_to_kg_weight_g` ne peuvent pas être effacés via l'API (COALESCE absorbe le JSON null). Décision Antho : défer V1 — workflow "effacer un champ posé" absent de l'UI V1 ; trigger Epic 4.2 réécrit ces colonnes de toute façon. Fix : remplacer COALESCE par `CASE WHEN p_patch ? 'field' THEN ... ELSE current_value END` pour les 4 nullable, en Epic 4.2 ou à la demande. [`20260424130000_rpc_sav_lines_prd_target_updates.sql:94-107`]

- **W1 — Race condition trigger `assign_sav_line_number`** : `SELECT COALESCE(MAX(line_number), 0) + 1` sans verrou advisory → deux inserts concurrents pour le même `sav_id` calculent le même next-value et l'un d'eux aborte sur UNIQUE violation. V1 safe (seul writer = capture_sav_from_webhook séquentiel). Fixer en Epic 4.2 : séquence par sav_id (via séquence PG dédiée) ou advisory lock `pg_try_advisory_xact_lock(sav_id)`. [`20260424120000_sav_lines_prd_target.sql:152-157`]
- **W2 — SECURITY DEFINER sans `SET search_path = public, pg_temp`** : pattern pre-existing sur toutes les RPCs SECURITY DEFINER du codebase (non introduit par Story 4.0). Supabase Cloud isole les schémas mais la best-practice Supabase recommande le pin explicite. Scope : refactor global Epic 7 (audit RGPD / sécurité). [`20260424130000_rpc_sav_lines_prd_target_updates.sql`]
- **W3 — `capture_sav_from_webhook` non-idempotent** : un retry Make.com double les lignes `sav_lines` (pas d'`ON CONFLICT` sur les lignes). Behavior pre-existing non modifié par Story 4.0. Solution : idempotency key sur `(sav_id, product_code_snapshot, position)` ou guard sur `sav.reference` déjà existant. À traiter avec l'intégration Make.com (Epic 2 carry-over ou Epic 7). [`20260424130000_rpc_sav_lines_prd_target_updates.sql:193-226`]
- **W4 — `p_expected_version` déclaré `int` vs `bigint`** : `update_sav_line(... p_expected_version int ...)` alors que `sav.version` est `bigint`. Implicit cast PostgreSQL fonctionne correctement en V1 (version < 2^31). Aligner le type au prochain `CREATE OR REPLACE` de la RPC (Epic 4.2 ou cleanup Epic 7). [`20260424130000_rpc_sav_lines_prd_target_updates.sql:32`]

## Deferred from: code review of 4-0b-dette-tests-sql-rpc-epic-3 (2026-04-23)

- **W5 — `ON CONFLICT ... DO NOTHING RETURNING id` retourne NULL silencieusement sur conflit** : `transition_sav_status` remonte `email_outbox_id=NULL` indistinctement quand (a) l'email a été skippé (F59 email vide) et (b) l'email a été dédup (F51 pending existant). Caller ne peut pas discriminer. Pré-existant Epic 3 CR F51, comportement intentionnel à V1. Traiter en Epic 6 (dispatcher email) si le caller a besoin de la distinction. [`20260423120000_epic_3_cr_security_patches.sql` + `20260424140000_rpc_variable_conflict_use_column.sql`]
- **W6 — F61 `GET DIAGNOSTICS ROW_COUNT=0` défense théorique** : le CAS verrou via SELECT FOR UPDATE rend ROW_COUNT=0 pratiquement inatteignable en sync. Défense en profondeur contre triggers concurrents hypothétiques. Test behavioral impossible en SQL sync (couverture = Epic 4.6 load 10k avoirs). Pré-existant Epic 3 CR. [`transition_sav_status`]
- **W7 — `update_sav_line` edge cases pré-existants** (hors scope tests 4.0b) : (a) patch individuel de `position`/`line_number` peut violer UNIQUE(sav_id, line_number) sans reorder ; (b) pas de `GET DIAGNOSTICS` entre UPDATE sav_lines et UPDATE sav.version → data drift possible si race DELETE concurrente ; (c) cast numeric/bigint accepte `NaN`/`Infinity`/négatifs (validation Zod amont en place, défense DB absente). Traiter en Epic 4.2 moteur calcul ou cleanup sécurité Epic 7. [`20260424130000_rpc_sav_lines_prd_target_updates.sql:94-111`]
- **W8 — `RAISE EXCEPTION 'LINES_BLOCKED|ids=%', bigint[]` format `{1,2,3}` non-standard** : si un caller parse le pipe-delimited, les accolades/virgules du tableau peuvent déstabiliser le split. Pré-existant Epic 3 Story 3.5. Normaliser en Epic 4.2 au moment du carry-over error-reporting. [`transition_sav_status`]
- **W9 — Email subject concat non-escapé** : `'SAV ' || v_sav_reference || ' : ' || p_new_status`. Si `reference` contenait du user-controlled data (non V1, la reference est générée par trigger), header/HTML injection possible downstream. Epic 6 (dispatcher HTML) gère le templating final. [`transition_sav_status`]

## Deferred from: code review of 4-2-moteur-calculs-metier-typescript-triggers-miroirs-fixture-excel (2026-04-25)

- **W16 (BH2)** — Conversion double-round precision drift sur `weight` non-divisible (ex: 201g). `creditCalculation.ts:108,112` et trigger PG font `round` sur `price_effective` intermédiaire puis re-multiplient × qty × coef → dérive potentielle de quelques cents sur grosses commandes. Les fixtures V1 n'exercent pas ces cas. Défer shadow run Epic 7 avec cas réels Excel — si dérive > 1 cent observée, refacto : `credit = round(qty × price_raw × coef / divisor)` en une passe sans intermédiaire rounded.
- **W17 (BH3)** — `SET search_path=public,pg_temp` correct mais tables référencées non-qualifiées (`FROM sav_lines` au lieu de `FROM public.sav_lines`) dans `recompute_sav_total`. Cohérent pattern Stories 4.0/4.1 mais durcissement cross-epic Epic 7 audit sécurité. [`20260426120000_triggers_compute_sav_line_credit.sql:1009,1118`]
- **W18 (BH7+AA-8)** — `format('%s', numeric)` et template TS `${qty_invoiced_converted}` peuvent afficher précision stockée (`6.0000000000000` vs `6`) dans les messages `validation_message`. Cosmétique UX V1.1 : formatter explicite `to_char(qty, 'FM999999.999')` côté PG et `.toFixed(3).replace(/\.?0+$/, '')` côté TS. [`creditCalculation.ts:122`, `migration trigger`]
- **W19 (BH14+P14)** — `sqlLiteral` dans le générateur ne gère que number/string/null. Ajouter support boolean/bigint/array et assertion `Number.isFinite` quand fixture V1.1 introduira de nouveaux types. [`scripts/fixtures/gen-sql-fixture-cases.ts:2437-2441`]
- **W20 (BH15)** — Test 11 SQL `UPDATE sav_lines SET qty_invoiced = qty_invoiced` est un no-op (assertion `v_credit_before = v_credit_after`) qui passe aussi si le trigger ne fire pas. Améliorer : UPDATE avec valeur changée puis retour à la valeur initiale (2 mutations, force le trigger à tourner 2 fois avec résultats identiques). [`trigger_compute_sav_line_credit.test.sql:1507`]
- **W21 (BH17)** — Tests SQL Tests 1-7 n'appliquent pas `DELETE FROM sav_lines` entre chaque DO bloc — `sav.total_amount_cents` cumule silencieusement. Aucun test n'assert le total avant Test 8 donc pas de régression, mais fragilité : un futur Test 2b qui assert total se brisera pour coupling raison. Refacto test isolation Epic 7. [`trigger_compute_sav_line_credit.test.sql`]
- **W22 (EC-17)** — Table `settings` ne ferme pas `valid_to` sur l'ancienne version quand on insère une nouvelle avec même `key`. Plusieurs rows peuvent être « en vigueur » simultanément → `resolveSettingAt` prend la plus récente par `valid_from` comme mitigation. Durcissement écran admin Epic 7 Story 7.4 (UI versioning settings). [`20260419120000_initial_identity_auth_infra.sql:171`]
- **W23 (EC-19)** — ESLint `no-restricted-imports` est statique et local : un dev peut mettre l'IO dans `api/_lib/clients/ioHelper.ts` puis l'importer depuis `_lib/business/` — la règle ne voit pas l'import transitif. Mitigation Epic 7 : test Vitest qui analyse l'AST de `_lib/business/**/*` et check les imports transitifs. [`client/package.json` eslintConfig override]
- **W24 (EC-22)** — TS `to_calculate` vs PG `check_violation` quand `coef > 1 AND price NULL` : TS short-circuit sur to_calculate (path §1), PG CHECK sur `credit_coefficient` fire avant le trigger. Divergence théorique rarement atteinte (Zod valide coef en amont), mais documentée.
- **W25 (EC-23)** — `computeCreditNoteTotals` accepte seulement des HT non-null. Contrat explicité Story 4.4 handler : filter `credit_amount_cents !== null AND validation_status === 'ok'` avant appel. Éventuellement ajouter overload qui prend `SavLineComputed[]` et filtre en interne. [`vatRemise.ts:589-598`]
- **W26 (EC-28)** — UPDATE `sav_lines SET sav_id=X` change le SAV mais `recompute_sav_total` ne recalcule QUE le NEW.sav_id — l'ancien garde un total stale. Pas un flow V1 (whitelist `update_sav_line` exclut `sav_id`). Durcissement Epic 7 : trigger `BEFORE UPDATE` qui raise si `OLD.sav_id <> NEW.sav_id`. [`20260426120000_triggers_compute_sav_line_credit.sql:1124`]
- **W27 (EC-30)** — `computeCreditNoteTotals` retourne un seul `vat_cents` agrégé, sans split par taux TVA. Story 4.5 (template PDF bon SAV) aura besoin du détail par bracket (550bp / 2000bp) pour la mention légale française multi-taux. Ajout signature retournant `{ vatByRateBp: Record<number, number>, ... }` Story 4.5. [`vatRemise.ts:ComputeCreditNoteTotals`]
- **W28 (BH20)** — `resolveSettingAt` tie-break : 2 rows avec même `valid_from` → première-vue wins (ordre Supabase non déterministe). Occurrence très unlikely (migrations sérialisent), mitigation Epic 7 UI admin force `valid_from >= old + 1ms`. [`settingsResolver.ts:793-805`]

## Deferred from: code review of 4-1-migration-avoirs-sequence-transactionnelle-rpc (2026-04-24)

- **W10 — `audit_changes()` sans `SET search_path` ni qualification `public.audit_trail`** : pré-existant Epic 1 (`20260419120000_initial_identity_auth_infra.sql:40-100`). La RPC `issue_credit_number` est bien pinned search_path, mais le trigger hérite du search_path appelant. Défense-en-profondeur incomplète : un attaquant créant `pg_temp.audit_trail` pourrait intercepter l'INSERT. Surface étendue par chaque nouvelle table audit-trackée (credit_notes). Durcir cross-epic : `SET search_path = public, pg_temp` sur `audit_changes` + qualifier explicitement `public.audit_trail` dans l'INSERT. [`20260419120000_initial_identity_auth_infra.sql:40-100`]
- **W11 — `audit_trail.diff` conserve `member_id` après anonymisation NFR-D10** : `row_to_json(NEW)::jsonb` sur `credit_notes` inclut `member_id`. L'anonymisation RGPD efface `members.name/email` mais laisse `audit_trail.diff.after.member_id` → ré-identification par jointure externe (chain attack). À traiter Epic 7 RGPD : soit filtrer les colonnes PII dans `audit_changes`, soit purger sélectivement `audit_trail.diff` lors de l'anonymisation. [`20260419120000_initial_identity_auth_infra.sql:57,62`]
- **W12 — Preuve empirique rollback cross-tx** : les tests `issue_credit_number.test.sql` (Test 6/8) prouvent l'atomicité intra-tx (PL/pgSQL sous-transaction). La vraie preuve NFR-D3 (2 backends PG concurrents, sérialisation `RowExclusiveLock`, zéro trou sur 10 000 émissions parallèles) = Story 4.6 (`scripts/load-test/credit-sequence.ts`). Gap explicitement scopé à Story 4.6. [`client/supabase/tests/rpc/issue_credit_number.test.sql`]
- **W13 — GUC `app.actor_operator_id` avec `is_local=true` + pooling transaction (pgBouncer)** : pattern hérité Epic 3 (toutes les RPC SECURITY DEFINER). En mode autocommit (connexion recyclée par pgBouncer entre 2 requêtes sans reset), la GUC pourrait persister côté backend. Aucun `RESET app.actor_operator_id` explicite en fin de RPC. Risque : trigger audit sur requête suivante logge le mauvais actor. À durcir cross-epic : audit de toutes les RPCs + reset explicite, ou migration vers une table de session. [`20260425130000_rpc_issue_credit_number.sql:61` + pattern Epic 3]
- **W14 — RLS `credit_notes_authenticated_read` clause (c) `NULLIF(current_setting('app.actor_operator_id', true), '') IS NOT NULL`** : un client authenticated direct positionnant `SET app.actor_operator_id = '1'` bypass le scoping member/group_manager → expose toute la base comptable. Pattern identique aux policies `sav_authenticated_read` Epic 3. Acceptable V1 car tous les endpoints passent par service_role (BYPASSRLS) + aucun client Supabase direct exposé. **À durcir impérativement Epic 6** quand le client Supabase direct adhérent/responsable sera exposé : ajouter `EXISTS (SELECT 1 FROM operators WHERE id = NULLIF(...)::bigint AND is_active)`. [`20260425120000_credit_notes_sequence.sql:215-220`]
- **W15 — Note PG17 IMMUTABLE à ajouter au PRD `prd.md:846`** : l'expression brute du PRD (`extract(year from issued_at)`) est refusée par PG17 en `GENERATED STORED`. L'implémentation 4.1 ajoute `AT TIME ZONE 'UTC'`. Le PRD doit soit être patché, soit annoté avec un pointeur vers cette Story pour rester source de vérité. Risque de confusion pour les futures stories qui liraient le PRD littéralement. [`_bmad-output/planning-artifacts/prd.md:846`]

## Deferred from: code review of 4-3-integration-moteur-vue-detail-preview-live (2026-04-24)

- **W29 — Test cross-stack DB↔TS (Task 4.4 Story 4.3)** : UPDATE via RPC, fetch détail, assert TS `linesComputed[i].credit_amount_cents === DB credit_amount_cents` sur fixture 4.2. Optionnel V1 per AC. Requiert environnement Supabase + RPC INSERT disponible en CI, à planifier quand l'intégration DB-CI sera prête. [`client/src/features/back-office/composables/useSavLinePreview.test.ts`]
- **W30 — Log dev-warn si shape jsonb settings inconnue** : le handler `detail-handler.ts` unwrap `.bp` puis passe la valeur telle quelle. Si une future migration introduit `{"basis_points": N}` ou un array, le handler retourne silently `null` dans `settingsSnapshot`. Ajouter un `logger.warn('settings.unknown_value_shape', { key, value_type })` pour catcher les drifts seed. [`client/api/_lib/sav/detail-handler.ts:72-83`]
- **W31 — ESLint override `useSavLinePreview.ts` ne bloque pas les imports relatifs vers `clients/supabase-admin`** : la règle `no-restricted-imports` ne couvre ni `XMLHttpRequest`, ni `WebSocket`, ni les ré-exports via chemin relatif `../../../../api/_lib/clients/*`. Defense-in-depth additionnelle : ajouter patterns relatifs au no-restricted-imports (ex: `**/clients/**`) et globals `XMLHttpRequest`, `WebSocket`. [`client/package.json` override `useSavLinePreview.ts`]
- **W32 — `settingsResolver.ts` ne gère pas les timestamps ISO malformés** : si `valid_from` ou `valid_to` est une string non parseable, `new Date(x).getTime()` retourne `NaN`, ce qui peut fausser la comparaison best-row silencieusement. Pré-existant Story 4.2, hors scope 4.3. À durcir : rejeter explicitement `Number.isNaN(fromMs)` dans `resolveSettingAt`. [`client/api/_lib/business/settingsResolver.ts:44-48`]

## Deferred from: code review of 4-4-emission-atomique-n-avoir-bon-sav (2026-04-24)

- **W33 (CR 4.4 D1) — Clock drift fenêtre settings `valid_from <= now AND (valid_to IS NULL OR valid_to > now)`** : pattern identique Story 4.3 `detail-handler.ts` (strict `>`) et 4.4 `emit-handler.ts`. Sur une transition de version settings où `valid_to_A = valid_from_B` à la seconde près, la fenêtre de matching peut renvoyer 0 row (si now tombe pile sur la borne), forçant un fallback silencieux ou un 500. Proba faible mais déterministe sur la borne. À traiter globalement : soit remplacer `>` par `>=` côté `valid_to`, soit permettre une tolérance 1s dans le resolver. Pré-existant 4.3, hors scope patch 4.4 immédiat. [`client/api/_lib/sav/detail-handler.ts:131-135` + `client/api/_lib/credit-notes/emit-handler.ts`]

## Deferred from: code review of 4-5-template-pdf-charte-fruitstock-generation-serverless (2026-04-24)

- **W34 — Retry smart error classification (`generate-credit-note-pdf.ts`)** : la boucle retry ×3 traite toutes les exceptions Graph API indifféremment (incluant 400 Bad Request, 413 Payload Too Large, assertion `"PDF dépasse 4 MB"`). Brûle 7s de lambda budget pour rien sur erreurs déterministes. À traiter : distinguer transient (5xx, 408, 429, 401 avec token refresh) du non-transient (400 sauf 408/429) et short-circuiter. [`client/api/_lib/pdf/generate-credit-note-pdf.ts:1801-1832`]
- **W35 — Retry refresh token MSAL sur 401 (`onedrive-ts.ts` + `graph.js`)** : si le token expire mid-retry, les 3 tentatives échouent toutes 401 au lieu de récupérer via un refresh. Ajouter détection 401 → `__resetForTests`-like refresh (ou meilleure : bound lifetime en amont). [`client/api/_lib/graph.js` + `client/api/_lib/onedrive-ts.ts:uploadCreditNotePdf`]
- **W36 — Installer `@vercel/functions` en dep production** : V1 utilise fallback `void p.catch(...)` qui ne garantit PAS la continuation post-response en serverless Vercel (lambda peut freezer avant résolution). Soit installer la dep (solution propre), soit migrer vers queue DB polling (W30 déjà défer). Impact : en prod sans `@vercel/functions`, tous les PDFs risquent de finir en `PDF_GENERATION_STALE` après 5 min. Tester en préview avant merge. [`client/package.json` + `client/api/_lib/pdf/wait-until.ts`]
- **W37 — Partial unique index `settings (key) WHERE valid_to IS NULL`** : aujourd'hui rien n'empêche 2 versions actives simultanées d'une même clé settings (admin oublie de bump `valid_to` de l'ancienne). `resolveSettingAt` choisit le `valid_from` max mais c'est non-déterministe sur égalité ms. Ajouter : `CREATE UNIQUE INDEX settings_one_active_per_key ON settings(key) WHERE valid_to IS NULL;` dans une migration future. Cross-cutting (toutes les clés, pas seulement `company.*`). [`client/supabase/migrations/` global]
